import 'package:flutter/material.dart';

class Repoststate extends StatelessWidget {
  const Repoststate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
